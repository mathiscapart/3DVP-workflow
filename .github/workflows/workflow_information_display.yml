name:  Workflow information_display
on:
  workflow_dispatch:

jobs:
  information_display:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice:: Ceci est une notice."
          echo "::warning:: Ceci est un avertissement."
          echo "::error::  Une erreur est survenue."