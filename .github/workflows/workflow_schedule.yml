name: Workflow Schedule
on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  say_hello_cron:
    name: "say_hello_cron"
    runs-on: ubuntu-latest
    steps:
      - run: | 
          echo 'Trigerred by cron: ${{ github.event.schedule }}'